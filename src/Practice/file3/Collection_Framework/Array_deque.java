package Practice.file3.Collection_Framework;
///✅ Insert
//addFirst(E e)   // insert at front
//addLast(E e)    // insert at rear
//offerFirst(E e) // insert at front (returns false if fails instead of exception)
//offerLast(E e)  // insert at rear
//
///✅ Remove
//removeFirst()   // removes first element (throws exception if empty)
//removeLast()    // removes last element
//pollFirst()     // removes first element (returns null if empty)
//pollLast()      // removes last element
//
///✅ Access / Peek
//getFirst()      // gets first element (throws exception if empty)
//getLast()       // gets last element
//peekFirst()     // looks at first (null if empty)
//peekLast()      // looks at last
//

import java.util.List;

///✅ Stack-like methods
//push(E e)       // push at front
//pop()           // pop from front
public class Array_deque {
    public static void main(String[] args) {

        List<Integer> list1 = List.of(1, 2, 3, 4);
    }
}
